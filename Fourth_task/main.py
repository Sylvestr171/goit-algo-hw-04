
#->->->->->->->->->->***FOURTH TASK***<-<-<-<-<-<-<-<-<-<

print ('\n\tFOURTH TASK\n')

# The 
def total_salary(path:str) -> tuple[int, float]:
    '''
    The 
    
    Input:
    :param date: string

    Output:
    :return: tuple[int, float]

    '''
   